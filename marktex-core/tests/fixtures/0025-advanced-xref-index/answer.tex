\emph{深度学习}\index{深度学习}
\emph{深度学习（Deep Learning）}\index{深度学习}\index{Deep Learning}
\emph{深度学习（Deep Learning, DL）}\index{深度学习}\index{Deep Learning}\index{DL}

\emph{硬件在环（HIL）}\index{硬件在环}\index{HIL}
\emph{HIL（硬件在环）}\index{HIL}\index{硬件在环}
\emph{HIL（硬件在环, Hardware-In-the-Loop）}\index{HIL}\index{硬件在环}\index{Hardware-In-the-Loop}

\emph{错误范例 (Wrong Case)}\index{错误范例 (Wrong Case)}
\emph{错误范例（Wrong Case，WC）}\index{错误范例}\index{Wrong Case，WC}

\emph{不完整的（别管了}\index{不完整的（别管了}
\emph{多个逗号（取第一个, 别取第二个,   更别取第 3 个）}\index{多个逗号}\index{取第一个}\index{别取第二个, 更别取第 3 个}
\emph{多个括号（别取前面的）（取最后一个）}\index{多个括号（别取前面的）}\index{取最后一个}。
\emph{不完整的（都别管了）（别管了}\index{不完整的（都别管了）（别管了}
\emph{不配对的（从后往前看（就可以了, OK）}\index{不配对的（从后往前看}\index{就可以了}\index{OK}。
